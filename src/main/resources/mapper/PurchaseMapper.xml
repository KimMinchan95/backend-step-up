<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.training.back.purchase.mapper.PurchaseMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO purchases (product_id, quantity, unit_price)
        VALUES (#{productId}, #{quantity}, #{unitPrice})
    </insert>

    <select id="findById" resultType="dev.training.back.purchase.model.Purchase">
        SELECT id, product_id, quantity, unit_price, purchased_at, status, canceled_at
        FROM purchases
        WHERE id = #{id}
    </select>
</mapper>